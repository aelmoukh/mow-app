package com.mowitmow.model.instruction;

public enum InstructionDeplacement implements Iinstruction {
    A // avance la tondeuse d'une case dans la direction à laquelle elle fait face, et
      // sans modifier son orientation
}
